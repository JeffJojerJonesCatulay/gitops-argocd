apiVersion: v1
kind: Service
metadata:
  name: real-world-app-release-service
  namespace: release
spec:
  selector:
    app: real-world-app-release
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 80
  type: NodePort
