apiVersion: v1
kind: Service
metadata:
  name: real-world-app-dev-service
  namespace: dev
spec:
  selector:
    app: real-world-app-dev
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: NodePort
