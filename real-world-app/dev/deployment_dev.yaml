apiVersion: apps/v1
kind: Deployment
metadata:
 name: real-world-app-dev
 labels:
   app: real-world-app-dev
spec:
 replicas: 1
 selector:
   matchLabels:
     app: real-world-app-dev
 template:
   metadata:
     labels:
       app: real-world-app-dev
   spec:
     containers:
     - name: real-world-app-dev
       image: jeffjojerjonescatulay/real-world-app:v1.0.1
       ports:
       - containerPort: 80